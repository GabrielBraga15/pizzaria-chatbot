version: '3.8'

services:
  bot-python:
    build: .
    container_name: bot_empresa_1
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always